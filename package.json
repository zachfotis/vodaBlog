{
  "name": "vodablog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:build": "cd client && npm run build",
    "server:build": "cd server && npm run build",
    "docker:run": "npm run client:build && npm run server:build && docker-compose up -d"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-leaflet": "^4.2.1"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.3"
  }
}
